#include "../include/bits.h"

int rightRot(int x, int n)
{
    return 0;
}

// This one is way easier to work out on paper, would implement this first if I where you
unsigned char rightRotChar(unsigned char x, int n)
{
    return 0;
}